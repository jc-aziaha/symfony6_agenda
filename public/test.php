<?php

    // $token_system = bin2hex(random_bytes(100));

    // var_dump($token_system);

    // if($token_system == $token_form)
    // {
    //     // contienr
    // }

    // throw new Exception